name: Hunt for Heisentests

on:
  workflow_dispatch:
  # every 15 minutes
  #schedule:
  #  - cron: "*/15 * * * *"

jobs:
  hunt:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.save.outputs.json }}

    steps:
    - name: Checkout Master
      uses: actions/checkout@v6

    - name: Setup Submodule
      run: |
        git submodule update --init --recursive

    - name: Update Engine Submodules
      run: |
        cd RobustToolbox/
        git submodule update --init --recursive

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v4.1.0
      with:
        dotnet-version: 10.0.x

    - name: Install dependencies
      run: dotnet restore

    - name: Build Project
      run: dotnet build --configuration DebugOpt --no-restore /m

    - name: Run Content.Tests
      id: tests
      shell: pwsh
      run: dotnet test --no-build --configuration DebugOpt Content.Tests/Content.Tests.csproj -- NUnit.ConsoleOut=0 NUnit.TestOutputXml="logs" NUnit.WorkDirectory="$(pwd)/test_results"

    - name: Run Content.IntegrationTests
      id: integration_tests
      shell: pwsh
      run: |
        $env:DOTNET_gcServer=1
        dotnet test --no-build --configuration DebugOpt Content.IntegrationTests/Content.IntegrationTests.csproj -- NUnit.ConsoleOut=0 NUnit.MapWarningTo=Failed NUnit.TestOutputXml="logs" NUnit.WorkDirectory="$(pwd)/test_results"

    - name: Extract Failures
      if: failure()
      id: get_fails
      run: |
        python -m pip install --upgrade pip
        pip install xmltodict
        python Tools/extract_failures.py

    - name: Debugging
      if: failure()
      shell: pwsh
      run: echo ${{ fromJSON(steps.get_fails.outputs.failures) }}

    - name: Store matrix
      if: failure()
      id: save
      uses: nickofthyme/object-remap@v3
      with:
        include.*.name: ${{ toJSON(fromJSON(steps.get_fails.outputs.failures).*.name) }}
        include.*.fullname: ${{ toJSON(fromJSON(steps.get_fails.outputs.failures).*.fullname) }}
        include.*.message: ${{ toJSON(fromJSON(steps.get_fails.outputs.failures).*.failure.message) }}
        include.*.output: ${{ toJSON(fromJSON(steps.get_fails.outputs.failures).*.output) }}

  report:
    needs: [ hunt ]
    if: ${{ always() && failure() }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        value: ${{fromJSON(needs.hunt.outputs.matrix)}}
    steps:
      - name: Echo name
        run: |
            echo "name: ${{ matrix.name }}"
      - name: Echo full name
        run: |
            echo "full name: ${{ matrix.fullname }}"
      - name: Echo message
        run: |
            echo "message: ${{ matrix.message }}"
      - name: Echo output
        run: |
            echo "output: ${{ matrix.output }}"
